{
  "name": "@ufrj/__TAG_NAME__",
  "version": "0.0.0",
  "main": "lib/index.js",
  "module": "lib/__CLASS_NAME__.js",
  "license": "AGPL-3.0",
  "author": "Vinícius Carvalho <vinicius.fc93@gmail.com> (https://ufrj.br)",
  "contributors": [],
  "scripts": {
    "clean": "run-p clean:*",
    "clean:build": "run-p clean-*",
    "clean:package": "rimraf *.tgz",
    "clean:cache": "rimraf .rpt2_cache",
    "clean-demo": "rimraf demo",
    "clean-lib": "rimraf lib",
    "build": "run-s clean:build build-all",
    "build-all": "run-p build:*",
    "build:typescript": "tsc -p ./config/tsconfig.build.json",
    "build:webcomponent": "rollup -c ./config/rollup.config.build.js",
    "watch-clean": "run-p clean:cache clean:package clean-demo",
    "watch": "run-s watch-clean build:typescript watch:*",
    "watch:webcomponent": "rollup -c ./rollup.config.js -w"
  },
  "dependencies": {
    "@ufrj/mnv-utils": "^0.0.0",
    "lit-element": "^2.2.0"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "rimraf": "^2.6.3",
    "rollup": "^1.15.6",
    "typescript": "^3.5.2"
  },
  "publishConfig": {
    "access": "public"
  },
  "babel": {
    "presets": [
      "@babel/preset-typescript"
    ]
  },
  "mnv": {
    "rollup": {
      "input": "./src/index.html",
      "outputDir": "../../public",
      "external": []
    }
  }
}
